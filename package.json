{
  "name": "anatomy-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Interactive 3D anatomy viewer (v0 vertical slice — skeletal upper limb).",
  "license": "CC-BY-SA-4.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "validate": "node scripts/validate-manifest.mjs",
    "fetch:bone": "node scripts/fetch-bodyparts3d.mjs",
    "convert": "node scripts/convert.mjs",
    "postinstall": "node scripts/copy-draco.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "ajv": "^8.17.1",
    "gltf-pipeline": "^4.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
